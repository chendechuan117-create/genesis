# Intent Analysis Protocol

## Mission
Analyze user input to identify core intent and relevant memory keywords.

## Output Format (JSON Only)
Return a single JSON object.

```json
{
  "core_intent": "Brief description of user goal",
  "memory_keywords": ["keyword1", "keyword2"]
}
```

Analyze the following input:
{{user_input}}
