<p align="center">
  <h1 align="center">Genesis 2.0</h1>
  <p align="center"><b>自觉型轻量级 AI Agent — 驻留在你的 Linux 桌面中</b></p>
  <p align="center">
    <img src="https://img.shields.io/badge/Python-3.12+-blue?style=flat-square" alt="Python">
    <img src="https://img.shields.io/badge/LLM-DeepSeek_V3-green?style=flat-square" alt="LLM">
    <img src="https://img.shields.io/badge/Memory-SQLite+Vector-purple?style=flat-square" alt="Memory">
    <img src="https://img.shields.io/badge/License-MIT-orange?style=flat-square" alt="License">
  </p>
</p>

---

Genesis 不是一个聊天机器人。它是一个**驻留在你的宿主系统中、拥有 Shell 权限、能自主诊断和解决问题的数字生命体**。

它融合了三种灵感来源的优势，用不到 3000 行核心代码实现了完整的自主 Agent 能力：

| 灵感来源 | 借鉴了什么 |
| --- | --- |
| **nanobot** | 极简架构，ReAct 循环 |
| **OpenClaw** | 文件式记忆系统，工具生态 |
| **纯粹元认知协议** | 多框架推理，降维攻击 |

---

## 核心架构

Genesis 采用 **Triad（三位一体）** 处理流水线，每次请求经过三个阶段：

```
用户输入
  │
  ▼
┌─────────────────────────────────────┐
│  ① 洞察者 (The Oracle)              │  意图识别 + 记忆搜索
│     识别核心意图，拉取相关记忆        │  → 轻量 LLM 调用
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  ② 裁决者 (The Strategist)          │  元认知战略制定
│     基于纯粹元认知协议生成攻击路径    │  → 多框架枚举 + 降维
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│  ③ 执行者 (The Body)                │  ReAct 循环
│     推理 → 调用工具 → 观察 → 再推理  │  → 弹性迭代 (最多 25 轮)
└────────────┬────────────────────────┘
             │
             ▼
    记忆持久化 (双写 SQLite + Markdown)
```

---

## 功能特性

### 🧠 记忆系统

- **深层记忆**：每次对话无条件持久化到 SQLite（语义向量可搜索）+ 每日 Markdown 日志（人类可读）
- **表层索引**：LLM 自动提取高价值决策模式（Situation→Action→Result→Insight）
- **跨会话连续性**：服务重启后自动加载近期对话日志，无缝衔接上下文
- **元模式合成**：定期回顾历史记忆，合成跨对话的用户行为模式

```
~/.nanogenesis/
├── qmd_memory.sqlite          # SQLite + FTS5 + 语义向量
├── conversations/
│   ├── 2026-02-10.md          # 每日对话日志 (人类可读)
│   └── 2026-02-11.md
└── data/
    └── adaptive_learning.json # 自适应学习数据
```

### 🔧 工具生态

| 工具 | 功能 | 说明 |
| --- | --- | --- |
| `shell` | 执行系统命令 | 支持超时保护、沙箱隔离 |
| `file_tools` | 文件读写 | 读取、写入、搜索文件 |
| `browser` | 网页访问 | 自动适配桌面/无头环境 |
| `web_search` | 网络搜索 | DuckDuckGo 搜索 |
| `memory_tool` | 记忆操作 | 搜索/添加持久化记忆 |
| `scheduler` | 定时任务 | 延迟执行、周期调度 |
| `skill_creator` | 自我扩展 | 运行时创建新工具并持久化 |

### 🌐 Web UI

内置异步任务式 Web 界面：

- **异步提交**：消息发送后立即返回，后台处理不阻塞
- **实时日志**：右侧 SSE 日志面板，实时显示 Genesis 的思考过程
- **Glassmorphism 设计**：Catppuccin 配色 + JetBrains Mono 字体

### ⚡ 自优化引擎

- **提示词自优化** (`PromptOptimizer`)：每 50 次交互自动优化 system prompt
- **工具调用优化** (`ToolUsageOptimizer`)：记录成功序列，复用高效路径
- **自适应学习** (`AdaptiveLearner`)：观察交互模式，动态调整行为策略
- **上下文压缩** (`CompressionEngine`)：长对话自动分块压缩，节省 Token

---

## 快速开始

### 环境要求

- Python 3.12+
- Linux 桌面环境 (Arch/EndeavourOS 测试通过)
- DeepSeek API Key

### 安装

```bash
git clone https://github.com/your-repo/Genesis.git
cd Genesis/nanogenesis

python -m venv venv
source venv/bin/activate
pip install -e .
```

### 配置

在 `~/.nanogenesis/config.json` 中设置你的 API Key（或通过环境变量）：

```bash
export API_KEY="sk-your-deepseek-api-key"
```

### 使用方式

**CLI 交互模式**（推荐调试）：

```bash
python cli.py "帮我检查一下为什么 Docker 服务启动失败"
```

**Web UI**（推荐日常使用）：

```bash
python server.py
# 打开 http://localhost:8000
```

**Systemd 常驻服务**：

```bash
# 创建用户级服务
cat > ~/.config/systemd/user/genesis-web.service << EOF
[Unit]
Description=Genesis AI Web Interface

[Service]
ExecStart=/path/to/venv/bin/python /path/to/server.py
Restart=always

[Install]
WantedBy=default.target
EOF

systemctl --user enable --now genesis-web.service
```

---

## 项目结构

```
nanogenesis/
├── agent.py                 # 主类：Triad 流水线 + 记忆持久化
├── server.py                # 异步 Web Server (HTTP + SSE)
├── cli.py                   # 命令行交互界面
├── daemon.py                # 后台守护进程
│
├── core/                    # 核心引擎
│   ├── base.py              # 基础接口 (Tool, LLMProvider, Message)
│   ├── loop.py              # ReAct 循环 (弹性迭代 + 死循环检测)
│   ├── context.py           # 上下文构建器 (Pipeline + 压缩 + 记忆注入)
│   ├── context_pipeline.py  # 插件式上下文管道
│   ├── memory.py            # QmdMemory (SQLite + FTS5 + 向量搜索)
│   ├── compression.py       # 历史记录分块压缩引擎
│   ├── provider.py          # LLM Provider (NativeHTTP / LiteLLM)
│   ├── registry.py          # 工具注册表
│   ├── representation.py    # 协议压缩
│   └── trust_anchor.py      # 信息源信任标记
│
├── tools/                   # 工具集
│   ├── shell_tool.py        # Shell 命令执行
│   ├── file_tools.py        # 文件读写
│   ├── browser_tool.py      # 网页访问 (自适应桌面/无头)
│   ├── web_tool.py          # 网络搜索
│   ├── memory_tool.py       # 记忆操作
│   ├── scheduler_tool.py    # 定时任务
│   └── skill_creator_tool.py # 运行时工具创建
│
├── intelligence/            # 智能层
│   ├── adaptive_learner.py  # 自适应学习器
│   └── prompts/             # 提示词模板
│       ├── intent_recognition.txt
│       └── pure_metacognition_protocol.txt
│
├── optimization/            # 自优化引擎
│   ├── prompt_optimizer.py
│   ├── behavior_optimizer.py
│   └── tool_optimizer.py
│
└── web/
    └── index.html           # Web UI (Glassmorphism + SSE 日志)
```

---

## API 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/chat` | 提交任务，立即返回 `{task_id}` |
| `GET` | `/api/task/{id}` | 轮询任务状态和结果 |
| `GET` | `/api/logs` | SSE 实时活动日志流 |
| `GET` | `/api/status` | 系统健康检查 |

**示例**：

```bash
# 提交任务
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "检查系统磁盘使用情况"}'
# → {"task_id": "a1b2c3d4", "status": "queued"}

# 轮询结果
curl http://localhost:8000/api/task/a1b2c3d4
# → {"status": "done", "result": "...", "metrics": {...}}
```

---

## 设计哲学

1. **行动优先**：遇到问题先动手诊断（跑命令、查日志），而不是推测
2. **记忆即文件**：所有对话以 Markdown 持久化，人类可读、可编辑、可版本控制
3. **信任 AI**：不用 10 条规则约束 AI 的行为，给出核心指导让它自然判断
4. **递归解决**：遇到困难自动拆解，直到解决或撞上物理限制
5. **自我扩展**：现有工具不够时，运行时创建新工具并持久化

---

## 灵感致谢

- **nanobot** — 极简架构灵感（用最少的代码做最多的事）
- **OpenClaw** — 文件式记忆、工具生态、SOUL.md 设计理念
- **纯粹元认知协议** — 多框架推理与降维攻击思想

---

## License

MIT
